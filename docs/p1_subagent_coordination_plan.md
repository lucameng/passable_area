# P1 Subagent Coordination Plan

## 1. Scope

This document tracks P1 work after the repaired P0 baseline
`fe6146f fix: prevent obstacle overlap from polluting support`.

P1 must preserve the P0 publication contract:

- no obstacle point publication for `height <= max_step_up`
- no global `base_gravity` floor clamp
- no bag-, ROI-, or frame-specific fixes
- no hidden parameter changes
- core logic remains ROS-free

## 2. Progress

| Agent | Status | Write files | Implementation summary | Tests added/updated | Validation results | Blockers |
| --- | --- | --- | --- | --- | --- | --- |
| P1-A Dropout Semantics Lead | Committed | `include/passable_area/core/types/frame_types.hpp`, `src/core/frame_observability_estimator.cpp`, `test/core/test_processor.cpp`, `docs/algorithm_scheme.md`, `docs/p1_subagent_coordination_plan.md` | Generalized empty-sector dropout detection to evaluate both front and rear hemispheres. `rear_dropout` remains rear-only for compatibility; new core `front_dropout` records front starvation, while directional dropout continues to flow through `sector_states` as `MissingByDropout`. No obstacle publication, support-reference, or bridge code changed. | Added rear-only/front-gap regressions covering front `MissingByDropout`, front support persistence under dropout, and rear boolean compatibility. Updated sparse/rear tests to assert `front_dropout` is not set. | `colcon build --packages-select passable_area --symlink-install`: pass. `colcon test --packages-select passable_area --event-handlers console_direct+`: pass. `colcon test-result --verbose`: `123 tests, 0 errors, 0 failures, 0 skipped`. False-obstacle benchmark over all six frozen bags: all pass with `suspicious_frames: 0`. Miss acceptance: Setup A left `1/4`, Setup A right `2/4`, Setup B left `2/3`, Setup B right `0/3`. Review: no blocking findings. Commit `a2c9d71 fix: add symmetric dropout sector detection`. | None. |
| P1-B Low-Clearance State Lead | Committed | `include/passable_area/core/obstacle_reasoner.hpp`, `src/core/obstacle_reasoner.cpp`, `test/core/test_obstacle_reasoner.cpp`, `docs/algorithm_scheme.md`, `docs/p1_subagent_coordination_plan.md` | Low-clearance geometry now requires an active overhead state before producing `LowClearance` / `Mixed` block reasons. `clearance` remains the geometric quantity; `overhead_confidence` / `overhead_evidence` carry state validity; publication still separately requires overhead evidence threshold and `clearance > max_step_up`. No config or publication height-gate changes. | Added reasoner tests for low-clearance geometry without overhead state, active state below publish evidence, and decayed overhead state below active threshold. | `colcon build --packages-select passable_area --symlink-install`: pass. `colcon test --packages-select passable_area --event-handlers console_direct+`: pass. `colcon test-result --verbose`: `126 tests, 0 errors, 0 failures, 0 skipped`. False-obstacle benchmark over all six frozen bags: all pass with `suspicious_frames: 0`. Miss acceptance: Setup A left `1/4`, Setup A right `2/4`, Setup B left `2/3`, Setup B right `0/3`. Review: no blocking findings. Commit `c61d5d1 fix: gate low clearance by overhead state`. | None. |
| P1-C Support Reference Consistency Lead | Committed | `src/core/processor.cpp`, `src/tools/miss_obstacle_analyzer.cpp`, `test/tools/test_miss_obstacle_analyzer.cpp`, `docs/algorithm_scheme.md`, `docs/p1_subagent_coordination_plan.md` | Removed the current-frame sample-min fallback as an obstacle publication support reference. Native obstacle point publication and miss-obstacle analysis now require finite trusted map `support_height`; analyzer height gates include the same strict `max_sample_z > support_ref + max_step_up` condition used by runtime publication. Planck's support provenance repair remains intact. | Added miss-obstacle analyzer tests for finite-support step-range gating and for no sample-min support fallback when `support_height` is missing. | `colcon build --packages-select passable_area --symlink-install`: pass. `colcon test --packages-select passable_area --event-handlers console_direct+`: pass. `colcon test-result --verbose`: `128 tests, 0 errors, 0 failures, 0 skipped`. False-obstacle benchmark over all six frozen bags: all pass with `suspicious_frames: 0`. Miss acceptance: Setup A left `1/4`, Setup A right `2/4`, Setup B left `2/3`, Setup B right `0/3`. Review: no blocking findings. Commit `1e78565 fix: require finite support for obstacle publication`. | None. |
| P1-D Terrain Geometry Lead | Committed | `src/core/terrain_feature_updater.cpp`, `test/core/test_processor.cpp`, `docs/algorithm_scheme.md`, `docs/p1_subagent_coordination_plan.md` | Terrain slope now uses each neighbor's actual planar distance, so diagonal support neighbors use `sqrt(2) * resolution` instead of being treated as cardinal neighbors. Step-up, step-down, roughness, clearance, and obstacle publication paths are unchanged. | Added a focused terrain feature regression for diagonal slope distance while preserving step-up and roughness magnitudes. | `colcon build --packages-select passable_area --symlink-install`: pass. `colcon test --packages-select passable_area --event-handlers console_direct+`: pass. `colcon test-result --verbose`: `129 tests, 0 errors, 0 failures, 0 skipped`. False-obstacle benchmark over all six frozen bags: all pass with `suspicious_frames: 0`. Miss acceptance: Setup A left `1/4`, Setup A right `2/4`, Setup B left `2/3`, Setup B right `0/3`. Review: no blocking findings. Commit `67f15ee fix: use diagonal distance for terrain slope`. | None. |
| P1-R Strict Review Lead | Completed | None | Separate read-only review subagents inspected the actual git diff before each P1-B, P1-C, and P1-D commit. | None | Each stage review reported no blocking findings. | None. |
